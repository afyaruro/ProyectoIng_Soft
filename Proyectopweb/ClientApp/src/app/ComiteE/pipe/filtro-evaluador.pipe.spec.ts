import { FiltroEvaluadorPipe } from './filtro-evaluador.pipe';

describe('FiltroEvaluadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEvaluadorPipe();
    expect(pipe).toBeTruthy();
  });
});
